use geoverse::{GeoCache, GeoCacheConfigBuilder};

fn main() {
  // let config = GeoCacheConfigBuilder::default()
  //   .storage_flush_strategy(Storage)
  //   .build();

  // let geo_cache = GeoCache::new(config).init().unwrap();
}
